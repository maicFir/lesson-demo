// // import React from 'react';

// //这里的mycount是mapStateToProps中返回的mycount,第二个参数只能是dispatch
// //dispatch({type:'counts/add'})----这里的counts是models中的namespaces  add是方法
// const CountApp = ({mycount,dispatch})=>{
  

//    return(
//      <div className="count-container">
//           <div className="current">{mycount.current}</div>
//           <input type="button" onClick={() => { dispatch({type: 'counts/add'}); }} value="添加"/>
//           <input type="button" onClick={() => { dispatch({type: 'counts/redu'}); }} value="减少"/>
//      </div>
//    )
// }

// // Example.propTypes = {
// // };
// CountApp.propTypes = {};


//  export default CountApp;





// // function mapStateToProps(state) {
// //   return { count: state.current };
// // }
// // const CountState = connect(mapStateToProps)(CountApp);


// // export default CountState;
